pytest==9.1.1
pytest-cov==7.1.0          # 覆盖率（CI 中 pytest --cov 使用）
httpx==0.28.1
PyYAML==6.0.2

# ── 测试夹具生成（生产解析已改用 anydoc，这些库仅测试用）────────────────────────
pypdf==6.16.1              # 测试：生成 PDF 夹具（6.11 有 18 条 CVE，6.16.1 全部修复）
reportlab==5.0.0           # 测试：生成可被 anydoc 提取文本层的合成 PDF
python-docx==1.2.0         # 测试：生成 docx 夹具

# ── 代码质量与依赖安全检查 ─────────────────────────────────────────────────────
ruff==0.16.6               # 代码质量检查和格式化（lint + format 双检查）
mypy==2.3.1                # 类型检查（渐进式：仅门禁 pyproject [tool.mypy].files 范围）
pip-audit==2.10.1          # Python 依赖漏洞审计（CI 中对已装环境审计）
